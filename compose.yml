services:
  web:
    build: .
    restart: always
    ports:
      - "127.0.0.1:5000:5000"
    depends_on:
      - mysql
  mysql:
    image: "mysql:5.7"
    environment:
      MYSQL_ROOT_PASSWORD: "root"
      MYSQL_DATABASE: "emoji"
    volumes:
      - ./db:/var/lib/mysql
